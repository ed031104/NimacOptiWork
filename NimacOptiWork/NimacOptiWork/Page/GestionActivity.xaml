<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NimacOptiWork.Page.GestionActivty"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NimacOptiWork.Page"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <!-- Horas -->
            <ColumnDefinition Width="*"/>
            <!-- Eventos -->
        </Grid.ColumnDefinitions>

        <!-- Columna de horas -->
        <ItemsRepeater Grid.Column="0" ItemsSource="{x:Bind Horas}">
            <ItemsRepeater.Layout>
                <UniformGridLayout Orientation="Vertical"/>
            </ItemsRepeater.Layout>
            <ItemsRepeater.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Border Height="60" BorderBrush="LightGray" BorderThickness="0,0,0,1">
                        <TextBlock Text="{x:Bind}" VerticalAlignment="Top" Margin="5,0,0,0"/>
                    </Border>
                </DataTemplate>
            </ItemsRepeater.ItemTemplate>
        </ItemsRepeater>

        <!-- Columna de eventos -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <Canvas x:Name="EventosCanvas" Height="1440"/>
            <!-- 24h * 60px = 1440 -->
        </ScrollViewer>
    </Grid>


</Page>
