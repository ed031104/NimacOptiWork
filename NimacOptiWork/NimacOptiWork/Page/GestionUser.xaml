<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NimacOptiWork.Page.GestionUser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NimacOptiWork.Page"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">

    <Grid Padding="20 0 20 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Fila 0 -->
            <RowDefinition Height="*"/>
            <!-- Fila 1 -->
        </Grid.RowDefinitions>

        <StackPanel 
          Grid.Row="0"
          Padding="0 50 0 50"
          >
            <TextBlock FontSize="20" Text="Gestión de Usuarios" />
        </StackPanel>

        <Grid Grid.Row="1" BorderBrush="Gray" BorderThickness="1">
            <controls:DataGrid
              x:Name="dataGridUsers"
              AutoGenerateColumns="False"
              IsReadOnly="False"
              SelectionMode="Single"
              >

                <controls:DataGrid.Columns>
                    <controls:DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="50"/>
                    <controls:DataGridTextColumn Header="Nombre" Binding="{Binding Name}" Width="*"/>
                    <controls:DataGridTextColumn Header="Correo" Binding="{Binding Email}" Width="*"/>
                    <controls:DataGridTextColumn Header="Rol" Binding="{Binding Role}" Width="100"/>

                    <!-- Columna editar -->
                    <controls:DataGridTemplateColumn Header="Editar">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button 
                                  Click="editUserEventBtn"
                                  Tag="{Binding Id}"
                              >
                                    <Button.Content>
                                        <FontIcon Glyph="&#xE70F;" FontSize="16"/>
                                    </Button.Content>
                                </Button>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>

                    <!-- Columna eliminar -->
                    <controls:DataGridTemplateColumn Header="Eliminar">
                        <controls:DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button 
                                  Click="deleteUserEventBtn"
                                  Tag="{Binding Id}"
                              >
                                    <Button.Content>
                                        <FontIcon Glyph="&#xE74D;" FontSize="16"/>
                                    </Button.Content>
                                </Button>
                            </DataTemplate>
                        </controls:DataGridTemplateColumn.CellTemplate>
                    </controls:DataGridTemplateColumn>

                </controls:DataGrid.Columns>
            </controls:DataGrid>
        </Grid>

    </Grid>
</Page>
