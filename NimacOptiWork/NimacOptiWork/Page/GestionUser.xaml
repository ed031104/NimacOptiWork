<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NimacOptiWork.Page.GestionUser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NimacOptiWork.Page"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tv="using:WinUI.TableView"
    mc:Ignorable="d">

    <Grid Padding="20 0 20 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Fila 0 -->
            <RowDefinition Height="*"/>
            <!-- Fila 1 -->
        </Grid.RowDefinitions>

        <StackPanel 
          Grid.Row="0"
          Padding="0 50 0 50"
          >
            <TextBlock FontSize="20" Text="Gestión de Usuarios" />
        </StackPanel>

        <Grid Grid.Row="1" BorderBrush="Gray" BorderThickness="1">
            <tv:TableView
              x:Name="dataGridUsers"
              AutoGenerateColumns="False"
              IsReadOnly="False"
              >

                <tv:TableView.Columns>
                    <tv:TableViewTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                    <tv:TableViewTextColumn Header="Nombre" Binding="{Binding Name}" Width="*"/>
                    <tv:TableViewTextColumn Header="Correo" Binding="{Binding Email}" Width="*"/>
                    <tv:TableViewTextColumn Header="Rol" Binding="{Binding Role}" Width="100"/>

                    <!-- Columna editar -->
                    <tv:TableViewTemplateColumn Header="Editar">
                        <tv:TableViewTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button 
                                  Click="editUserEventBtn"
                                  Tag="{Binding Id}"
                              >
                                    <Button.Content>
                                        <FontIcon Glyph="&#xE70F;" FontSize="16"/>
                                    </Button.Content>
                                </Button>
                            </DataTemplate>
                        </tv:TableViewTemplateColumn.CellTemplate>
                    </tv:TableViewTemplateColumn>

                    <!-- Columna eliminar -->
                    <tv:TableViewTemplateColumn Header="Eliminar">
                        <tv:TableViewTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button 
                                  Click="deleteUserEventBtn"
                                  Tag="{Binding Id}"
                              >
                                    <Button.Content>
                                        <FontIcon Glyph="&#xE74D;" FontSize="16"/>
                                    </Button.Content>
                                </Button>
                            </DataTemplate>
                        </tv:TableViewTemplateColumn.CellTemplate>
                    </tv:TableViewTemplateColumn>

                </tv:TableView.Columns>
            </tv:TableView>
        </Grid>

    </Grid>
</Page>
