<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="NimacOptiWork.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NimacOptiWork"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="NimacOptiWork">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <!-- Columna izquierda -->
            <ColumnDefinition Width="3*" />
            <!-- Columna derecha -->
        </Grid.ColumnDefinitions>

        <!-- Panel izquierdo -->
        <StackPanel Grid.Column="0" Padding="40" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="20">

            <!-- Logo -->
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Image Source="/Assets/logoNimac.png" Height="40"/>
            </StackPanel>

            <!-- Descripción -->
            <TextBlock Text="Tu aliado para optimizar la productividad y la eficiencia"  
                FontSize="20"  
                FontWeight="SemiBold"   
                TextWrapping="Wrap"/>

            <TextBlock Text="Bienvenido de nuevo, por favor inicie sesión en su cuenta"  
                FontSize="14"  
         />

            <!-- Campos de login -->
            <StackPanel Spacing="10">
                <TextBlock Text="Correo"/>
                <TextBox x:Name="email" PlaceholderText="usuario@correo.com" />

                <TextBlock Text="Contraseña"/>
                <PasswordBox x:Name="password"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="5">
                <TextBlock VerticalAlignment="Center" Height="auto" Text="Se te olvdió la contraseña?" Foreground="Gray" Margin="0,10,0,0"/>
                <TextBlock VerticalAlignment="Center" Height="auto" Text="toca aquí" Foreground="Orange"/>
            </StackPanel>

            <!-- Botones -->
            <Button Click="LoginEventBtn" Background="Black" Foreground="White"  Content="Iniciar sesión" HorizontalAlignment="Stretch" Width="auto" />

            <InfoBar x:Name="infoBar"
      Title="Éxito"
      Message="Los datos se guardaron correctamente."
      IsOpen="False"
      Severity="Success"/>
        </StackPanel>

        <!-- Panel derecho: Imagen -->
        <Border Grid.Column="3">
            <Border.Background>
                <ImageBrush ImageSource="/Assets/backgroundNimac.jpg" Opacity="0.7" Stretch="UniformToFill"/>
            </Border.Background>
        </Border>


        <!-- Overlay de carga -->
        <Grid x:Name="loadingOverlay" Background="#80000000" Visibility="Collapsed">
            <ProgressRing Width="60" Height="60" IsActive="True"/>
        </Grid>
    </Grid>

</Window>
